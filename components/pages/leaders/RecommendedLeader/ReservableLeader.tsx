import styled from "@emotion/styled";
import React from "react";

type Props = {};

function ReservableLeader({}: Props) {
  return <Container>예약 가능한 리더</Container>;
}

const Container = styled.div``;

export default ReservableLeader;
